# 2026-09-11：0.8.2 悬浮条即时密度与轮廓修复稳定发布

关联`REQ-20260911-016`，交付`REQ-20260911-013`至`REQ-20260911-015`。[发布计划](../design/implementation-plans/2026-09-11-v0.8.2-release.md)、[Release Notes](../releases/v0.8.2.md)。

目标为双平台稳定版0.8.2、macOS build25。用户确认授权公开候选源码、PR、main合并、`v0.8.2`标签、GitHub Release、现有双平台签名流程和三个更新源；范围限定为三项已完成修复及必要的版本、验证和证据文档。

## 候选范围

- Settings切换三档密度后，已展开的macOS和Windows原生窗口立即采用新尺寸；Windows保存回执与对应原生协调轮次串行关联。
- 左贴边暴露侧上下角恢复圆润；贴屏侧上下肩使用更清晰的反向曲线，左右贴边及上下肩严格镜像。
- Mini和Compact贴屏侧肩深为70pt/px，Comfortable为88pt/px；30pt/px反向肩弧不改变圆环尺寸、Provider顺序、自动收起或拖动语义。

## 候选验证

0.8.2/build25候选通过499项Swift（478项普通、3项刷新调度、18项PTY）、137项Windows前端、254项宿主Rust、25项浏览器进程生命周期、8种Antigravity详情、24组四Provider展开、2组收起态、608个文字角色、production build、rustfmt、严格Clippy、六份跨平台合同、284份Markdown、公开安全和无Widget arm64严格签名Release App验证。

受`AI_METER_SCREEN_TESTS=1`控制的真实AppKit回归共6项通过，其中`NSPanel`从Compact 78pt立即调整到Mini 65pt，保持中心与贴边位置。真实Chrome密度门禁在允许本机回环监听后通过；宿主Rust中使用隔离回环服务的DeepSeek测试也在允许本机监听后完整通过。首次沙箱运行分别因`listen EPERM`失败，未修改产品代码、未跳过或放宽断言。

等待精确候选的独立差异复核以及PR与main双平台原生CI证据。

首次独立候选复核为Critical/Important/Minor 0/1/0：README和发布流程提前把尚未执行的0.8.2写成当前公开稳定版，与项目状态和发布待办冲突。候选已改为明确区分当前公开0.8.1与源码0.8.2/build25；待Release实际公开后再随发布后证据切换公开事实。

## PR、main与签名发布

等待候选PR、main合并提交、`v0.8.2`标签与正式发布workflow证据。

## 公网验收

等待七项公开资产的匿名重下、SHA-256、Sparkle/Tauri签名、篡改拒绝和三个更新入口验证。

## 验收边界

物理Windows 11的WebView2、真实指针、多显示器与125%/200% DPI仍属于现场验收边界。macOS自动化覆盖SwiftUI/AppKit结构、绘制和窗口控制，但不把未执行的人工观察冒充自动验证。Windows仍无Authenticode；macOS公开包仍为Apple Silicon、ad-hoc、未公证且不含Widget。真实账号数据不进入自动验证。
