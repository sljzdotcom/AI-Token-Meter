# 2026-09-07：0.4.0 双平台稳定发布

关联 REQ-20260907-005；[执行计划](../design/implementation-plans/2026-09-07-v0.4.0-release.md)、[Release Notes](../releases/v0.4.0.md)。

## 准备

目标 0.4.0 / macOS build 12。源功能来自 PR #9，合并 `054e3f3`。main macOS CI `34075597338` 与 Windows CI `34075597491` 均成功。

当前状态：2026-09-07 10:44（Asia/Singapore）已公开 [v0.4.0](https://github.com/sljzdotcom/AI-Token-Meter/releases/tag/v0.4.0)，非草稿、非 Preview，GitHub latest 为本版。采用既有干净 main 顺序发布入口，不改变信任根，不读取/导出私钥，不自动安装维护者本机。

版本检查点及 Tag `v0.4.0` 指向 `15d80e2`。正式打包再次通过 macOS 405+13 项测试、资源完整性、Sparkle 嵌入、严格签名、归档 EdDSA/篡改拒绝和公开安全扫描。双平台发布 workflow [34076547278](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34076547278) 的 macOS 标签验证、Windows 全量测试/正式安装器签名与同步公开三个任务全部成功；appcast 提交 `2d2254f`。

发布预检：168 份 Markdown、跨平台版本合同、当前内容及完整 Git 历史公开安全扫描通过；57 项 Windows 前端、21 项生命周期、632 项实际 Chrome 计算样式及 production 构建通过，本机 Rust 全量通过。首次浏览器尝试因沙箱禁止 localhost 监听而 EPERM；放行环境后完整通过，不属于产品失败，也未跳过断言。版本断言与启动时初始版本同步为 0.4.0。

## 验收边界

macOS 不包含 Widget，仍 ad-hoc 且未公证；Windows 有 updater 签名但无 Authenticode。物理多屏/DPI 验收和间歇性 PTY/ConPTY 时序问题继续按原需求独立追踪。

## 公网复验

匿名重新下载七份公开资产和三个更新入口，不使用 GitHub 账户凭据。全部校验通过，未重建或替换公开包。

| 安装包 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `AI-Token-Meter-0.4.0-macOS-arm64.zip` | 3611320 | `6fe52e92bd7588bb9251bfe91d105716f6023371bafceae9903497a0eb021196` |
| `AI-Token-Meter-0.4.0-windows-x64-setup.exe` | 5095778 | `7004f53db3147d878419869eeda10e7be4e8b1427b9b95e79fe95c2a5174bc90` |

- 两端文件与公开 SHA-256 一致；macOS Sparkle 验签和篡改拒绝通过，解压后严格 codesign 验证通过，版本/build 为 0.4.0/12。
- Windows 使用应用嵌入公钥验证安装器 `.exe.sig` 通过，清单签名与该签名文件一致。
- 稳定 raw appcast 与 Release appcast 字节相同；Windows latest 清单、旧 Preview 固定清单及 Release latest.json 三者字节相同，版本均为 0.4.0，地址均指向已验证公开安装包。
- 0.3.0 与 0.4.0 更新地址、公钥完全相同。支持更新器的旧设备无需重新配置；0.1.2 没有更新器，仍需手动安装一次。
- 本轮验证的是公开资产与更新发现链路，没有远程操作各台机器实际安装，不宣称完成物理双屏或 Windows 原位升级验收。

发布需求已完成，安装指南、设置说明、README、项目状态、提交索引及需求台账同步。历史待验收和延期项保持原状态。
