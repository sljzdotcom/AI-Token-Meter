# Claude 详情隐私说明移除开发验收日志

**日期：** 2026-09-02
**需求：** `REQ-20260902-009`
**结果：** 已完成、安装并通过真实辅助功能树验收

## 背景与目标

Claude 详情在本机每日趋势下方显示一条带 `lock.shield` 图标的英文隐私说明。用户选择方案 A，要求整条移除，不保留占位、悬停提示或设置开关。

## 实现与边界

- 删除 `ClaudeDetailView.localActivitySection` 中的隐私 `Label`；
- 不改变官方额度、Sessions、Active days、Tokens、每日趋势、空状态和更新时间；
- 不改变 `ClaudeLocalActivityReader` 白名单、资源上限、领域模型、缓存或旧数据兼容；
- 用户指南与隐私文档继续说明真实数据边界。

## 测试先行证据

先新增“隐私说明必须缺席”的源码契约，再在旧生产代码上运行定向测试：

- 新测试产生 **2 个预期失败断言**；
- 失败原因分别是说明文字和 `lock.shield` 仍存在；
- 删除 `Label` 后，Claude 详情展示测试 **5/5 通过**。

最终完整测试结果：

- **295 项测试、58 个测试组通过；**
- **0 个失败。**

## Release、安装与真实桌面验收

使用 `AI_METER_INCLUDE_WIDGET=0` 构建候选包：

- `codesign --verify --deep --strict` 通过；
- 主程序为 arm64 Mach-O；
- 候选版和安装版主程序 SHA-256 均为 `fa23f95a948d17b217c12e6522fb1aa57a25eb8518c8ab88b3950d8b04cd190b`；
- 覆盖前版本备份在 `/private/tmp/AI Token Meter-pre-privacy-note-cleanup-20260902-0813.app`。

安装后点击 Claude，辅助功能树显示：Claude 标题、两项官方额度、Sessions、Active days、Tokens、每日趋势和 Updated；隐私文字及锁图标均未出现。按 Escape 后详情由 `Detail open` 回到 `Detail closed`。

辅助功能焦点会按产品设计暂停自动隐藏，因此本次实机交互不把焦点保持期间未自动关闭记为失败；完整回归套件继续覆盖自动隐藏会话与关闭行为。

## 安全与隐私

本次删除的是可见说明，不是保护逻辑。没有新增网络、凭证、日志或数据字段，也没有扩大 Claude 本机历史读取范围。

## Git 节点

- `64a2b6f`：确认完整移除的设计边界；
- `3f36f7a`：书面规格确认后进入开发；
- `54e223f`：保存测试驱动实施计划；
- `2ce2146`：移除隐私说明并增加缺席回归测试；
- `a7b8e06`：同步用户文档、验收日志和需求完成状态。

## 已知边界

- 非激活 `NSPanel` 的像素截图仍可能只捕获浮岛；本次使用真实辅助功能树验证详情内容，没有把不完整截图当作证据；
- Widget 证书和 Gallery 验收继续由 `REQ-20260901-003` 单独跟踪。
