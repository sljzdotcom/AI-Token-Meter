# 2026-09-10：0.7.0 → 0.7.1 Google Antigravity 与紧凑界面稳定发布

关联`REQ-20260910-004`与`REQ-20260910-005`，交付`REQ-20260909-007`、`REQ-20260909-011`和`REQ-20260910-001`。[发布计划](../design/implementation-plans/2026-09-10-v0.7.0-release.md)、[0.7.1 Release Notes](../releases/v0.7.1.md)。

目标为双平台稳定版0.7.1、macOS build20。用户在确认Compact最终采用65pt后明确要求“请发布”；授权范围包含必要推送、PR、合并、标签、双平台签名资产、公开GitHub Release和三个应用内更新入口。0.7.0/build19首次标签工作流在公开前停止，恢复不移动既有标签。

## 发布范围

- 第四项服务迁移为Google Antigravity，使用官方`agy -p /usage`展示Gemini与Claude/GPT四个额度窗口，并无损保留内部兼容标识、用户排序和缓存。
- Compact浮动条由78收至65pt/px，48pt/px圆环、垂直尺寸、Comfortable和折叠把手保持。
- macOS菜单面板应用Logo由32pt放大至40pt，其他布局与Windows菜单保持。
- 同期发布Windows Codex与ConPTY原生测试夹具可靠性修复；产品超时、真实协议和安全边界未降低。

## 65pt功能整合

[PR #26](https://github.com/sljzdotcom/AI-Token-Meter/pull/26)最终候选`c199c6c`的macOS CI 34449356259用时2分1秒，Windows CI 34449356218用时14分50秒；合并提交`1c0a1eddff52ba3b472d83b7f86ac7616e9eb5a6`的main macOS CI 34450634215用时2分46秒，Windows CI 34450634472用时11分27秒，均通过完整门禁。

## 发布候选、签名事务与公网验收

版本合同先按失败先行改为0.7.0/build19：生产仍为0.6.3/build18时，Swift版本合同7处与Rust应用元数据1处准确失败；同步根版本、两份macOS plist、Windows npm/Cargo/Tauri及锁文件后，Swift定向13项和Rust元数据1项转绿。

v0.7.0标签已经由首次事务推送，因此恢复版使用0.7.1/build20，不移动既有标签。恢复合同先期待0.7.1/build20：Swift准确报告旧0.7.0/build19共7处不符，Rust元数据测试报告0.7.0不等于0.7.1；同步生产元数据后，Swift定向13项和Rust元数据1项转绿。

本机候选`9bdc352`完成以下门禁：

- 462项普通Swift、3项刷新调度、18项PTY runner，共483项；6份跨平台合同、243份Markdown和公开安全检查通过。
- Windows 124项前端、241项宿主Rust、25项浏览器生命周期、8个Antigravity状态、16组布局与608个文字角色通过；production build、Rust格式与严格Clippy通过。
- 无Widget Apple Silicon Release App为0.7.0/build19、arm64；便携资源、Sparkle framework/helper、加载路径、嵌套组件和严格签名通过。
- 对版本单调性、发布范围、更新兼容、凭据边界、回滚与公开说明完成结构化差异复核，Critical/Important/Minor为`0/0/0`。

0.7.1恢复候选随后通过本机完整门禁：463项普通Swift、3项刷新调度和18项PTY runner，共484项；Windows前端124项、宿主Rust 241项、浏览器25项生命周期、8个Antigravity状态、16组布局及608个文字角色通过，production build、Rust格式与严格Clippy均通过。6份合同、245份Markdown、公开安全检查，以及0.7.1/build20的arm64无Widget Release App资源、Sparkle组件与严格签名均通过。

## 首次标签工作流失败

[PR #27](https://github.com/sljzdotcom/AI-Token-Meter/pull/27)最终候选`a268728`的macOS/Windows CI `34453043998`/`34453044040`分别用时2分36秒与11分38秒通过；合并提交`28690fdae57cab53b73e29ed269d14e6b5f5a28f`的main CI `34454132543`/`34454132537`分别用时2分31秒与11分7秒再次通过。

首次标签workflow [`34455443116`](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34455443116)中，macOS标签源码与草稿签名资产验证用时2分46秒通过。Windows签名任务的前端阶段在两个jsdom测试文件的首项分别运行9.679秒与10.848秒，超过Vitest默认5秒上限；其余122项通过，后续浏览器、Rust、NSIS、签名、上传与公开步骤按门禁全部跳过。v0.7.0 Release保持草稿，三个公开更新源仍指向0.6.3。可靠性修复由`REQ-20260910-005`跟踪；恢复不移动或重写已经推送的v0.7.0标签。

## 0.7.1恢复候选与原生CI

恢复候选提交`c06ae71`经[PR #28](https://github.com/sljzdotcom/AI-Token-Meter/pull/28)通过macOS CI [`34457708387`](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34457708387)与Windows CI [`34457708465`](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34457708465)，用时2分11秒与11分52秒。PR合并为`c79a5f2115137b7e47b82370d996f0fae0a023b6`后，main macOS CI [`34458853540`](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34458853540)用时2分43秒，Windows CI [`34458853613`](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34458853613)用时11分35秒，均完成前端、真实浏览器、严格Rust、完整运行时、NSIS、GUI subsystem和安装器上传。

## 正式发布与公网验收

注解标签`v0.7.1`指向`c79a5f2`。[正式发布workflow `34460178399`](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34460178399)中，macOS标签与草稿签名资产验证用时1分54秒；Windows签名任务用时17分58秒，完成124项前端、真实Edge、完整Rust、NSIS、GUI subsystem、资产规范化、SHA-256、内嵌公钥验签和上传；同步发布任务用时16秒。Release于2026-09-10 09:39:37 UTC公开，稳定appcast由`85b281d`提交到main。v0.7.0草稿随后删除，标签继续指向首次候选`28690fd`，没有移动或重写。

[Release v0.7.1](https://github.com/sljzdotcom/AI-Token-Meter/releases/tag/v0.7.1)提供七项公开资产；无认证重下结果如下：

- macOS ZIP 3,756,332字节，SHA-256 `af398eb96347a6d677d818905ff706dbe550168c69bb39c3b3ad2b9112d97c96`；解包后为0.7.1/build20、arm64，严格嵌套签名通过。
- Windows安装器5,238,759字节，SHA-256 `998952114a43480ff20f9aafec95959f9b07439373612b781712bd39b8e17b1c`；`.sig`为428字节，使用应用内嵌Tauri公钥验证通过。
- Release appcast为2,531字节，SHA-256 `64a9fe628ff58db6732e1e73d891682b4fafa6171eb635cf3c931bc4f112fb9b`；Sparkle验签通过，追加字节的篡改ZIP被拒绝。
- Windows `latest.json`为763字节，SHA-256 `67f28709deb880644e827d4958da439df49a1578eab4ccdab33c4813e1dc1347`；篡改安装器被拒绝。
- 根raw appcast与Release appcast逐字节一致；Release stable `latest.json`、`releases/latest/download/latest.json`和固定`windows-preview-feed/latest-preview.json`逐字节一致，均指向0.7.1。

公开资产、更新签名、篡改拒绝和三个更新入口全部通过，现有macOS稳定版、Windows稳定版与旧Preview入口均可发现0.7.1。

## 现场边界

- Windows交互式菜单、多DPI与SmartScreen仍需真实用户会话观察；自动化覆盖Windows runner、NSIS和GUI subsystem。
- Windows仍无Authenticode；macOS仍为Apple Silicon、ad-hoc、未公证、无Widget公开包。
- 不代用户登录或安装Google Antigravity CLI；真实账号额度继续保留现场验收边界。
