# 2026-09-08：0.5.1 Windows CLI 修复发布

关联 REQ-20260908-006；[计划](../design/implementation-plans/2026-09-08-v0.5.1-release.md)、[Release Notes](../releases/v0.5.1.md)。

基于 main `80184f3`，产品修复合并 `aca64fc` / PR #12。目标稳定 0.5.1/macOS build14，沿用同版本双平台 Release、Windows stable 与旧 Preview 入口，以及既有 Tauri/Sparkle 公钥。

状态：2026-09-08 12:38:14（UTC+8）公开为 latest 稳定版 0.5.1/build14。没有新功能改动，不自动安装维护机器，不登录/退出用户账号，不导出私钥。实际账号额度与初始化保留现场验收边界。

版本准备时，既有 Widget 元数据测试捕获扩展 plist 漏同步（仍 0.5.0/build13）的两条断言；补齐到 0.5.1/build14，不跳过测试、不启用 Widget。README 版本/下载指导也由文档门禁要求同步，公开前明确标注准备发布。依赖版本和历史损坏 appcast fixture 保持不变。

## 构建与公开证据

- 版本准备及 Tag `v0.5.1`：`132287b`；正式 appcast：`eac027a`。
- [发布 workflow 34186704869](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34186704869) 的 macOS、Windows、公开发布三项 job 全部成功。
- 本机及标签 macOS：420 项主测试 + 13 项 PTY、完整合同/文档/公开安全通过；Release 便携资源、嵌套签名、Sparkle 验签及篡改拒绝通过，未嵌入 Widget。
- Windows 标签：85 项前端、21 项密度生命周期、632 个浏览器文字角色、229 项原生 Rust、严格 Clippy、GUI subsystem、NSIS 签名构建与原内嵌公钥验签通过。
- [公开 Release](https://github.com/sljzdotcom/AI-Token-Meter/releases/tag/v0.5.1) 为 `draft=false`、`prerelease=false`、latest，共七项安装包/哈希/签名/feed 资产。

## 匿名更新验收

不携带 GitHub 登录凭据下载全部七项资产、raw appcast、Windows stable 与旧 Preview feed；验证日志 `/private/tmp/v051-public-verification.log`。全部通过：

| 资产 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| AI-Token-Meter-0.5.1-macOS-arm64.zip | 3642337 | `04152d54191bd484a06f9b17ffe02cce52573d80ad3d695adbd2c01b218ba023` |
| AI-Token-Meter-0.5.1-windows-x64-setup.exe | 5146507 | `3aa6b4f7bd28b43a12103b8315dad4e90b2e1a027941ee89f5290da28ad48c03` |

- Windows 两个实际更新入口均返回 0.5.1，URL 指向本版公开 EXE；manifest 签名与 `.sig` 一致。原内嵌公钥验证下载的 EXE 成功，添加字节的篡改副本被签名验证拒绝。
- 与 `v0.5.0` 配置比较，应用标识、更新地址、公钥不变，版本单调递增；更新前稳定清单为 0.5.0，更新后为 0.5.1。
- macOS 公开 ZIP 与本机签名归档逐字节一致；appcast 与 raw 入口一致，首项 0.5.1/build14，长度/URL 正确，Sparkle 验签、篡改拒绝及解包后严格 codesign 全部通过。

REQ-20260908-006 已完成。已有 Windows 可在 设置 → 关于 → 检查更新 → 立即更新 升级。安装新版后先在服务卡检查状态；Claude 若仍需工作区设置，可显式初始化后再检查。REQ-20260908-001 的真实账号/工作区现场验收保持受环境限制，不以自动化或发布成功替代。
