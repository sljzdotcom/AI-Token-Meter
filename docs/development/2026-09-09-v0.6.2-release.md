# 2026-09-09：0.6.2 菜单面板 Logo 稳定发布

关联 `REQ-20260909-004`，交付 `REQ-20260909-002`。[发布计划](../design/implementation-plans/2026-09-09-v0.6.2-release.md)、[Release Notes](../releases/v0.6.2.md)、[功能记录](2026-09-09-menu-panel-app-logo.md)。

目标为双平台稳定版0.6.2、macOS build17。用户在得知菜单Logo已完成但仍属Unreleased后，明确要求开发直接发布新版本；授权范围包含必要代码/文档推送、PR与原生CI、main整合、标签、双平台签名资产、公开GitHub Release和三个应用内更新入口。

## 发布范围

- macOS菜单栏弹出面板在标题块左侧显示32pt现有应用图标，保持透明背景、标题/副标题对齐和右侧刷新按钮。
- Windows原生托盘菜单首行显示同一默认图标与软件名，使用禁用品牌项和原生分隔线；四服务摘要及全部事件路由保持。
- 不新增图片资产、网络请求、偏好迁移、账号字段或凭据路径；版本身份与更新公钥保持兼容。

## 版本失败先行

- Swift元数据与跨平台合同先改为期待0.6.2/build17；旧生产文件准确产生7处版本/build不符。
- Rust应用元数据测试先期待0.6.2；旧嵌入版本准确报告左值0.6.1、右值0.6.2。
- 同步根`VERSION`、主App与Widget plist、Windows npm/Cargo/Tauri及锁文件后，14项Swift定向合同和1项Rust元数据测试转绿。完整门禁与发布审查在后续章节持续记录。

## 候选与发布门禁

- 远端最高稳定标签与Release为0.6.1，0.6.2未占用；build17高于已发布build16。
- 精确候选`b76585a`通过491项Swift：464项普通测试、3项独立刷新调度、18项PTY runner及6项Gemini PTY；6份跨平台fixture、便携性、Schema显示名及不可用额度合同同时通过。
- Windows候选通过18个前端文件的124项测试、5项Node终端协议测试、production build、25项真实Chrome生命周期、8个Gemini场景、16组四服务布局与608个文字角色；宿主Rust 261项、格式及全目标严格Clippy通过。
- 0.6.2/build17无Widget Release App已重新构建；主程序为Apple Silicon arm64，Sparkle 2.9.4通过`@rpath`加载，便携资源、framework/helper及主App严格嵌套签名全部验证通过。
- 版本合同、Windows资产归一化、Release feed、219份Markdown、公开安全及`git diff --check`通过；构建环境仅报告用户缓存/WebKit sandbox不可用警告，不影响产品断言或真实浏览器门禁。

## 发布审查

- 范围只包含已验收的双平台菜单Logo、0.6.2/build17元数据、对应测试和发布文档；发布脚本、workflow、应用身份、更新公钥、更新源地址、设置与凭据格式均未改变。
- macOS图标仅作装饰并对辅助功能隐藏；Windows品牌行不可点击且使用Tauri默认窗口图标。刷新、四服务摘要、设置、浮动条、关于、退出及事件路由保持原样。
- build17严格高于0.6.1/build16；根appcast和两条Windows feed在公开事务完成前仍保持0.6.1，避免清单提前指向不存在的资产。
- 差异检查未发现秘密、私钥、令牌、外部下载逻辑或破坏性迁移；发布失败继续由既有草稿、补偿与同版本恢复保护处理。
- 审查结果为Critical 0、Important 0、Minor 0；现场边界保持为Windows交互式菜单/多DPI、SmartScreen及macOS公证状态。
- 发布签名私钥不会被读取、导出、输出或写入仓库；只验证Sparkle工具/公钥和GitHub Actions Secret可用性。

## 当前阶段

精确本机候选和发布审查已通过，下一步推送PR并等待macOS、Windows原生CI。公开稳定版、根appcast、Windows stable及固定Preview feed在0.6.2资产公开并通过验证前继续保持0.6.1。

## 现场边界

- Windows交互式原生菜单观感、多DPI与SmartScreen仍需对应真实用户会话确认；自动化验证原生菜单项构建、Windows runner、NSIS和GUI subsystem。
- Windows仍无Authenticode；macOS仍为Apple Silicon、ad-hoc、未公证、无Widget公开包。
