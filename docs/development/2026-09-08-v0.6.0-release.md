# 2026-09-08：0.6.0 Gemini 与累积改进发布

关联 `REQ-20260908-016`，并交付 `REQ-20260908-007`、`REQ-20260908-009`、`REQ-20260908-010`、`REQ-20260908-011` 与 Gemini `REQ-20260908-012`。[发布计划](../design/implementation-plans/2026-09-08-v0.6.0-release.md)、[Release Notes](../releases/v0.6.0.md)。

目标为双平台稳定版 0.6.0、macOS build 15。用户已明确授权分支推送、原生 CI、必要修复与复审、main 整合、双平台签名构建、GitHub Release 和更新源发布，并进一步点名允许向公开仓库 `github.com/sljzdotcom/AI-Token-Meter` 推送、合并 PR #13、发布 `v0.6.0` 标签及应用内更新源，让现有安装可发现新版本。发布流程不代用户登录 Gemini、消耗真实额度或覆盖本机已安装应用。

## 发布范围

- Gemini 青绿第四按钮、四服务显示排序与旧配置迁移。
- 固定官方 Gemini CLI 0.58.0 普通 Google OAuth 的模型档位额度采集；Windows 首期仅原生 CLI。
- macOS 可编辑、保存并运行时应用用量刷新间隔。
- 双平台浮动条展开态移除顶部横线。
- 双平台 Telegram 图标链接、Windows 关于页作者精简和新版深红粗体提示。

## 候选证据

- Gemini 产品实现与复审提交：macOS `edd9cdd`、`f96b08c`；Windows `5a0cced`、`8675152`、`057e79f`；文档收尾 `be2f350`。
- 本地候选回归：461 项主 Swift + 19 项 PTY，共 480 项；109 项前端；16 组浮动条布局与 632 个浏览器文字角色；249 项宿主 Rust 与严格 Clippy。
- 官方 Gemini CLI 隔离合成回归：2 项测试、4 种生产 PTY 场景；不使用真实账号或真实额度。
- macOS Release 主应用构建及签名结构通过；缺少 Developer ID 时按既定公开边界生成 ad-hoc、无 Widget 包。
- Gemini 实现独立复审已关闭合法 ANSI 分片、清屏异常掩盖与父配置类型等全部发现。

## 发布候选独立审查

独立审查没有 Critical，并在 `46e6de7` 关闭四项 Important 与一项 Minor：GitHub Draft 现在强制使用 `docs/releases/v<version>.md`；macOS Codex 初始化从当前 App Bundle 取得客户端版本并有初始化帧回归；Gemini 登录文档改为实际提供的官方指南与重新检查；候选下载文案不再链接尚未公开的 Tag；macOS build 门禁改为只遍历语义化版本标签并比较历史最大 build，忽略固定 channel 标签。复核精确提交 `46e6de7d89d16dcfa2d0f7eafc2f70d7304ee966` 后，Critical、Important、Minor 均为 0，未发现新回归或凭据泄露。

修复后的完整本地回归为 461 项主 Swift + 19 项 PTY，共 480 项；官方 Gemini CLI 2 项测试/4 个隔离场景再次通过。无 Widget 的 macOS Release App 为 0.6.0/build 15，便携资源、Sparkle 嵌套组件和严格签名均通过；重新生成的候选 ZIP 通过公开发布安全扫描。Sparkle Keychain 公钥与 App 元数据一致，GitHub 上两项 Tauri 签名 Secret 名称均存在，未读取或输出私钥。

## 发布门禁执行记录

- PR #13 首轮 Windows CI `34226445772` 的 Vite 与 21 项生命周期回归通过，但 runner 内置 Google Chrome 在 45 秒内无 stdout/stderr、exit 或 close，随后完成有界清理；因此样式断言和 Rust/ConPTY/NSIS 尚未运行。该失败登记为 `REQ-20260908-017`，没有用原样复跑放行。
- 修复先增加“Chromium 内部 DOM 捕获期限必须早于外层进程看门狗”的失败回归，再按官方 headless 参数加入 10 秒 `--timeout`，保留原 45 秒外层看门狗和进程树清理。22 项生命周期、109 项前端、production build 与本机真实 Chrome 的 8 个 Gemini 状态、16 组四服务布局、632 个文字角色已通过，等待 PR 原生 Windows 复验。
- PR #13 第二轮 Windows 真实浏览器门禁已在约 12 秒内完成并进入严格 Clippy，证明内部捕获期限修复在原生 runner 生效。同期 macOS 的 461 项主测试通过，但单独 PTY 命令把 `PTYCommandRunnerTests` 与 `GeminiPTYTests` 两个资源密集 Suite 放在同一进程并行执行，随后出现父进程退出超时、并发输出缺失及 Gemini ready/PID 时序四项失败；首轮同候选 macOS 全绿，本轮仅改 Windows 脚本。该复发归入 `REQ-20260906-003`，将拆成两个独立测试进程并保留全部 19 项、原期限及原断言，不用成功复跑放行。
- 同一轮 Windows 严格 Clippy 在仅 Windows 条件编译的 `service_account_statuses` 分支报告 `needless_return`，使运行时、ConPTY、Job Object、NSIS 与 PE 门禁尚未运行。该阻塞登记为 `REQ-20260908-018`，以原生 `-D warnings` 失败作为红灯，只移除冗余返回，不增加 allow 或降低检查；等待下一轮 Windows CI 完成全部原生门禁。
- PTY 流程合同先对旧脚本得到 3 个预期失败，拆分后本机 461 项主测试、13 项 PTY runner 与 6 项 Gemini PTY 共 480 项通过；原断言和期限均保留。Windows 条件分支改为语义等价尾表达式后，宿主格式、严格 Clippy 与 249 项 Rust 测试通过；首次测试仅有 6 项回环服务器用例因沙箱禁止 bind 失败，获准环境以同一命令全部通过。207 份 Markdown 文档门禁也已通过，仍以 PR 原生结果作为最终放行依据。
- PR #13 第三轮 macOS CI `34228704438` 全绿，分进程的 461+13+6 项验证支持跨 Suite 资源竞争根因。Windows CI `34228704586` 的前端、真实浏览器、生产构建、格式及原 `needless_return` 已通过，严格 Clippy 随后在 Windows 专用 `cli_installation` 测试发现 URL 断言没有穷尽新增 Gemini 枚举；这项独立阻塞登记为 `REQ-20260908-019`。修复会把仅 Claude/Codex 可下载执行的安装脚本和期望 URL 建为显式测试表，保留 Gemini 只开放官方指南的边界，不用 wildcard、allow 或 skip；完整原生运行时、NSIS 和 PE 仍待下一轮实际执行。
- 安装测试现以 Claude/Codex 与各自官方 URL 的显式二元表驱动，不再对完整 `CliProvider` 枚举做不穷尽匹配，Gemini 仍不会执行安装脚本。宿主格式、严格 Clippy 与 3 项安装定向测试通过；本机虽已安装 Windows Rust target，交叉检查仍因缺少 Windows CRT 头文件而在第三方 `ring` 构建阶段停止，因此不把它写作 Windows 原生编译成功，继续等待 runner 复验。
- PR #13 第四轮 macOS CI `34229619225` 在 461 项主测试的 `RefreshIntervalSchedulingTests.replacesWaitingTimerAndRejectsLateCompletions` 等待条件处超时；这一轮只改 Windows 专用测试，第三轮同调度测试通过。该独立阻塞登记为 `REQ-20260908-020`，会保留计时器替换、旧完成拒绝、采集中不重叠的全部断言和期限，先检查具体条件及并行 Suite 的调度资源，不用成功复跑关闭。Windows 同轮仍继续运行，以保留它能达到的最深原生证据。
- 失败日志显示大量 `MainActor` 测试并行启动，多个无关 Suite 统一耗时约 8 秒；刷新调度用例尚未获得第一次模型任务推进机会时，两秒行为窗口已经过去。流程合同先得到 2 个预期失败，再把 `RefreshIntervalSchedulingTests` 的 3 项测试移入独立进程；完整本机门禁以 458 项普通测试 + 3 项刷新调度 + 13 项 PTY runner + 6 项 Gemini PTY 的分组通过，总数仍为 480，原调度断言和两秒期限均未改变。6 份跨平台合同、发布脚本回归、207 份文档和公开安全检查同步通过。
- 同轮 Windows 原生严格 Clippy 已全绿，证明 `REQ-20260908-018` 与 `REQ-20260908-019` 两处条件编译修复有效。运行时完成单元及前序集成测试后，`gemini_conpty_windows.actual_conpty_fixed_input_success_timeout_and_cancel_reap_the_child` 在第 88 行的固定输入成功场景得到 `TimedOut`，综合用例耗时 8.87 秒；NSIS 与 PE 按门禁没有运行。该失败属于既有 `REQ-20260906-003` 的 Windows ConPTY 可靠性范围，继续保留成功、超时、取消与子进程回收的全部验收，不通过延长截止、删除断言或重跑成功放行。
- 对照 `NativeTerminal` 的启动握手实现和历史 Claude 原生 CI `33719315118` 后，根因收敛为 Gemini 测试夹具把宿主回复子进程光标查询的 `ESC[1;1R` 与固定 `/model` 命令累计在同一个输入字符串中；夹具要求精确等于 `/model\r`，所以永远不输出模型页并最终超时。修复只从夹具业务输入中剥离这一固定终端协议回复，同时把原始输入单独落盘，并由 Windows 测试断言握手确实存在、清理结果准确、完整业务输入仍精确等于 `/model\r + Esc + /quit\r`。成功、退出冲突、超时、取消与子进程回收场景及期限均未删改，生产 ConPTY 代码未改。Node 语法、Rust 格式、严格 Clippy、24 项 Gemini 宿主定向测试、4 项 CI 流程合同和 207 份文档检查通过；Windows 条件综合用例继续等待 PR 原生 runner 复验。
- 精确提交 `08dc153` 的完整本机门禁另在 `FloatingStripRenderingTests` 得到 6 个 `brightLogoPixels == 0`；独立复审进程的同一门禁曾全绿，但没有用该成功复跑关闭问题。增加布局诊断后，两次聚焦红灯都只指向 compact 密度下 provider count 2/3/4 的 Codex Logo（左右两侧各一次）。PNG 显示 Logo 已绘制且位置正确；根因是 Gemini 布局扩展时把原有 2× Retina 位图改成 1×，Codex SVG 细线在 compact 尺寸经抗锯齿后最高亮度约 86%，所以原 92% 参考掩码为空。测试现恢复明确 2× 位图，并把尺寸、顶部玻璃带和 Logo 采样坐标统一从点换算为像素；92%/85% 阈值、四种 provider 数、两种密度、左右边缘和折叠截图均保留。聚焦矩阵及完整 458+3+13+6 共 480 项通过，随后 6 份跨平台合同、发布脚本、207 份文档和公开安全检查全绿，等待独立复审与 PR 最终提交 CI。
- PR #13 第五轮 Windows CI `34232052626` 在 `08dc153` 上通过前端、真实浏览器、production build、格式、严格 Clippy 以及 Gemini 前序 Rust 测试，但真实 ConPTY 综合用例的 success 场景仍于第 93 行在 8.88 秒后得到 `TimedOut`；NSIS、GUI subsystem 和 PE 门禁未运行。这一反证说明只剥离 `ESC[1;1R` 的夹具修复没有覆盖实际阻塞。原测试又在读取原始输入证据前先 `unwrap`，临时目录随失败展开被删除；现已让夹具记录最多六项去重且单调追加的阶段历史，并让测试在结果断言前读取该历史、输入总长度和最多 128 个合成输入字节，使下一轮原生 runner 在清理前留下安全的真实证据。独立复审先发现单值阶段会被后续普通输入覆盖，改为历史后额外输入不再抹掉已到达的模型阶段。Node 语法、Rust 格式、宿主严格 Clippy 和 207 份文档检查通过；8/2 秒期限、固定命令、四种结果和子进程回收断言保持不变。
- PR #13 第六轮 Windows CI `34237111918` 在 `69e842d` 上再次通过前端、真实浏览器、production build、格式、严格 Clippy 及 Gemini 前序 Rust 测试。ConPTY 成功场景的新诊断显示阶段停在 `started>ready-written>input-received`，原始和既有归一化输入均为 38 字节：`ESC[?61;6;7;21;22;23;24;28;32;42c/model\r`。Gemini 0.58.0 录制输出开头明确含 `ESC[c` 设备属性查询，Windows 伪控制台回送 DA 属性序列后才接收 `/model`；夹具只剥离固定光标位置回复，故精确业务分支不可能触发。失败先行新增 4 项 Node 回归，覆盖这段原生 DA 回复、既有固定光标回复、累积输入分片，以及不完整、异常和未支持控制序列必须保留；初次因归一化模块不存在按预期失败。实现只在合成夹具中剥离固定 `ESC[1;1R` 与参数全为十进制数的完整主设备属性回复。4 项 Node、109 项前端、cargo fmt、严格 Clippy、24 项 Gemini 宿主定向测试、跨平台合同、207 份文档和公开安全门禁均通过；生产 ConPTY/采集代码、固定命令、四种结果、8/2 秒期限和回收断言不变，等待独立复审与原生 Windows 完整复验。
- 独立审查精确提交 `c1b8b8e` 报告 1 项 Important：两阶段替换会先删除固定光标回复，再把原始流中被它隔开的两段异常 DA 拼成新的完整 DA 并误删，例如 `ESC[?61 + ESC[1;1R + ;6c`。其余测试接入、Rust 四场景、时序、子进程回收、有界诊断和无生产代码变更均通过。修复会新增这两种组合红灯，并在原始字符串上用一次扫描同时识别两类完整回复，随后复审新的精确提交；当前候选尚未推送放行。
- 两种组合回归先在 `c1b8b8e` 上稳定复现误删，再把两阶段替换改为单个 alternation 正则，对原始输入只扫描一次；删除完整光标回复后不会重新扫描两侧拼接出的异常 DA。5 项 Node、109 项前端、cargo fmt、严格 Clippy、24 项 Gemini 宿主定向测试、跨平台合同、207 份文档及公开安全检查全绿，等待独立复审修复后的精确提交。
- 独立复审精确提交 `d08ae29` 的 Critical、Important、Minor 均为 0，上一轮组合误删已关闭。PR 第七轮 macOS CI `34240652127` 全绿；Windows CI `34240652001` 通过 109+5 项测试、真实浏览器、production build、格式、严格 Clippy 及前序 Rust 后，真实 ConPTY success 场景在 1.69 秒得到 `UnrecognizedOutput`。有界诊断显示阶段已推进到 `model-opened`，原始输入是 DA + `/model\r` + DA，归一化准确为 `/model\r`，证明设备属性回复阻塞已修复；业务 Escape/quit 尚未出现，NSIS/PE 按门禁未运行。下一步核对模型页输出和会话状态机为何在关闭模型页前判定不可识别，继续保留原时限与断言。
- 宿主新增同一真实 40KB untrusted 模型页的 4096 字节与 137 字节读取边界回归，两种均取得 60% 并完成 `/model → Esc → /quit`，排除已测试的两种分片边界。Windows 专用综合测试现以包装层记录停止前实际输出、非空读取边界，以及每个边界的 terminal/quota 解析结果；失败日志仍只包含固定合成数据，并限制为总长度、96 字节头和 256 字节尾。下一轮原生结果将区分 ConPTY 输出转换与中间帧校验问题，不改生产解析、时限或验收。
- 精确提交 `bc813ed` 的 macOS CI `34242730700` 全绿。Windows CI `34242730653` 继续通过109+5项前端/协议测试、真实浏览器、production build、格式、严格Clippy及Gemini前序Rust测试；ConPTY诊断在16/1778/3859字节边界依次得到Waiting/Ready/Waiting，直到7778字节模型页帧才由terminal与quota解析共同返回UnrecognizedOutput。原始69字节输入含两段主设备属性回复，归一化仍精确为`/model\r`，且阶段已到`model-opened`、尚未发送Esc/quit。该证据排除输入协议、就绪识别及已测试的137/4096字节分片；原生新增3919字节的边界仍待定位其中首个不支持或经ConPTY转换的控制序列。NSIS与PE仍按门禁未运行。
- 精确提交 `579c6ec` 的 macOS CI `34244232472` 全绿；独立复审确认完整十六进制诊断仅作用于Windows合成fixture且原始字节上限16 KiB，Critical/Important为0。复审指出上条记录不能把137/4096字节宿主回归泛化为全部分片，现已收窄表述。Windows CI `34244232308` 捕获同样的7778字节模型页帧，其中唯一不受解析器支持的控制操作为12个`CSI n X`，首个是Unicode字符索引3654、UTF-8字节偏移4532处的`ESC[125X`；Microsoft Windows VT规范将`CSI n X`定义为ECH，即从当前光标起以空格擦除n个字符且不移动光标。下一步以完整帧和最小擦除语义先建立失败回归，再补这一项有界标准操作；不放开其他未知序列。
- 新增7778字节原生ConPTY合成帧回归及最小ECH语义用例，旧实现稳定得到2项UnrecognizedOutput红灯。解析器现仅增加`X`分支：把当前行中从光标起最多n个已存在单元改为空格，不扩展行、不移动光标；定向9项随即全绿。临时把`X`改成接受但不执行的无操作后，最小用例仍按预期失败，证明回归同时约束擦除效果和光标语义；随后已恢复正确实现。完整宿主Rust 251项、格式、严格Clippy、6份跨平台合同、207份文档和公开发布安全检查通过；第一次Rust全套的6项回环服务器用例仅因沙箱禁止bind失败，同一命令在获准环境全部通过。未知控制序列仍拒绝，完整Windows四场景、时限及回收合同未变，等待独立复审和原生CI。
- ECH实现独立复审为Critical/Important/Minor 0/1/1。Important指出`X`擦除分支没有执行既有“擦除前观察”安全条件，同一累计帧里的登录提示可能先被擦掉，再由后续Ready内容覆盖；Minor纠正了上述偏移单位。修复将先以会话级回归证明登录提示不能被ECH掩盖，再让`X`和`K`一样在修改当前行前触发观察，随后重新独立复审。
- 会话级回归以登录提示、回车、`CSI 29 X`和Ready提示构成同一累计帧；旧实现没有返回AuthenticationRequired并继续发送业务输入，稳定复现审查发现。`X`现在与`K`共用擦除前行观察，修复后立即拒绝该帧、没有发送任何命令；Gemini collector 9项与session 12项定向测试全绿，等待独立复审与完整门禁。
- 第二轮复审确认前一项Important及偏移单位Minor均已关闭，只发现新增测试源文件没有通过rustfmt这一项机械性Minor；格式化后`cargo fmt --check --manifest-path windows/src-tauri/Cargo.toml`已转绿，等待最终复核。
- 最终独立复审Critical/Important/Minor为0/0/0。审查者确认ECH擦除前观察、会话级登录阻塞、默认/零参数、边界、光标不移动、未知序列拒绝和完整fixture均符合约束；十六进制fixture解码后的帧为7778字节，SHA-256 `1b886acaa86e1d1946df7876adf889774ba7bb0a3bd6fbf8aaee80dc1dd8e656`，与Windows CI `34244232308`逐字节一致。独立执行的宿主Rust 252项、Gemini 9+12项、格式、严格Clippy、207份文档及diff检查均通过，放行提交与原生Windows CI。
- 本任务随后在最终源码上重新执行格式、严格Clippy及完整宿主Rust 252项，全数通过；6份跨平台合同、207份文档、公开发布安全检查和diff检查也均为绿色。候选具备提交并触发PR双平台原生CI的本地证据。
- 精确提交`dbb1fc6`的macOS CI `34247798922`全绿。Windows CI `34247798952`通过109+5项前端/协议测试、真实浏览器、production build、格式与严格Clippy；关键真实ConPTY综合用例首次在7.61秒完成成功、超时、取消及子进程回收四场景。随后同一`cargo test`进程中的合成分片会话回归仅在137字节路径于固定期限内得到TimedOut，4096字节路径及其余11项会话测试通过，故NSIS与PE仍未运行。下一步检查该回归的每帧重放工作量、期限和Windows调度影响，保留两种分片、命令顺序及60%结果，不原样复跑。
- 137字节路径把约40KB输出拆为约293次读取，每次会对累计终端内容重新校验；它验证分片正确性，不应与同一测试程序中的其他长会话回归争用两秒行为期限。Cargo本机文档确认不同集成测试target串行执行、单一target内测试多线程并行，因此只把原回归移入独立`gemini_fragmentation`测试程序。137/4096两种边界、两秒deadline、`/model → Esc → /quit`和60%断言均未改变；独立1项用0.83秒通过，原session剩余11项用0.50秒通过，等待独立复审与原生CI。
- 分片测试隔离修复独立复审Critical/Important/Minor为0/0/0。新target只含原高工作量回归，40,069字节fixture、137/4096对应293/10个分片、两秒期限、60%结果、完整命令顺序和停止断言全部保留；生产代码、Cargo配置和CI命令没有改变。审查者实际验证独立1项0.82秒、session 11项0.50秒、完整Rust 252项、格式、严格Clippy、207份文档及diff全绿，放行提交与原生Windows CI。
- 本任务在最终测试布局上重新执行完整宿主Rust 252项，输出确认独立`gemini_fragmentation` 1项用0.83秒完成，随后`gemini_session` 11项用0.50秒完成；格式、严格Clippy、6份跨平台合同、207份文档、公开发布安全与diff检查均通过，准备提交PR分支。
- 精确提交`935a199`的macOS CI `34249876942`全绿；Windows CI `34249876816`再次通过前端、浏览器、构建、格式与严格Clippy，真实ConPTY四场景也连续第二次用7.50秒全绿。独立`gemini_fragmentation`仍在137字节路径得到TimedOut，target总耗时2.18秒，证明移出并行session target还不足以进入Windows的原两秒期限；NSIS/PE继续按门禁未运行。下一步检查会话读取层能否合并当前已经排队的分片并减少293次累计重放，同时保留擦除前历史观察、137/4096边界、期限与命令/结果断言。
- 失败先行的会话安全回归把Ready、7段无视觉变化的待处理输出和登录提示同时放入非阻塞队列；旧实现虽最终识别AuthenticationRequired，却已在看到登录提示前发送完整`/model`，无输入断言稳定红灯。会话读取现先排空当前已就绪分片再对累计历史校验一次，排空期间每次读取前仍检查取消与原deadline，每次追加仍执行512KiB上限；`observe_frames`继续按顺序检查合并数据中的清屏、擦除和完整对话框，所以登录提示不能被后续帧隐藏。修复后该回归无输入即拒绝，137/4096两种分片、两秒期限、60%、完整命令顺序和stop断言均未改变；分片回归本机从约0.83秒降至0.04秒，session 12项用0.50秒全绿，等待独立复审与原生Windows CI。
- 该读取修复独立复审Critical/Important/Minor为0/0/0：逐段取消/deadline/512KiB限制、UTF-8不完整尾段、历史清屏/ECH/认证/冲突额度观察、稳定期、固定逐字节输入和清理语义均保留；新安全回归只从终端外部行为断言认证错误、零输入与停止，没有复制实现。审查者及本任务分别执行完整253项宿主Rust、格式、严格Clippy与定向测试全绿；本任务另完成458项普通+3项刷新调度+13项PTY runner+6项Gemini PTY共480项Swift、6份合同及失败回归、Windows资产/feed回归、207份文档、公开安全与diff检查，准备提交精确候选并进入双平台原生CI。
- 为避免当前就绪数据排空后让小帧分片测试只在一次校验中完成，测试终端现于每个非空块后显式返回一次空读取；有效额度与冲突历史仍分别按整帧、二分/整帧边界和逐字节跨独立burst解析，继续覆盖不完整UTF-8尾段与拆分ESC。大体量137/4096回归仍保持所有分片同时就绪来约束排空性能，新认证回归也保持同一队列来约束输入前观察。增量独立复审Critical/Important/Minor仍为0/0/0，定向session 12项、fragmentation 1项、格式、严格Clippy、207份文档和diff检查全绿。
- 精确提交`505d6d8`的macOS CI `34252423980`在合并的测试与文档步骤以exit 1失败，具体用例尚未读取；Windows CI `34252424048`仍在执行。先保留该精确失败并读取完整日志，不以同提交本机480项Swift全绿或此前macOS CI成功替代。
- 同一精确提交的Windows CI `34252424048`在真实浏览器密度步骤以exit 1失败，production build、Rust/ConPTY、NSIS与PE尚未运行；具体输出尚未读取。保留本轮失败并先取证，不用此前相同步骤成功或本机浏览器结果替代。
- macOS日志确认458项普通测试与3项刷新调度全绿；独立PTY runner 13项仅“父进程退出不能让脱离子进程保持PTY”失败。两秒deadline开始后，fixture Python直到1.592秒才启动、1.595秒才请求父进程退出，runner在2.474秒返回TimedOut；其余12项通过。下一步核对行为期限是否错误包含受runner冷启动影响的fixture准备阶段，同时保留真实父进程退出、脱离子进程与有界回收验收。
- Windows日志确认density build、22项生命周期合同和370毫秒preview启动全绿；Google Chrome随后在45秒外层期限内没有任何stdout/stderr/exit/close，10秒内部DOM timeout本轮未能促使进程结束，清理后总耗时51.875秒。后续production build、Rust/ConPTY、NSIS与PE均未运行。下一步检查浏览器调用参数及有界替代路径，仍保留真实浏览器文字/布局断言、45秒外层失败和进程树清理。
- macOS父进程退出fixture现用原生shell立即输出并退出，由忽略HUP的后台sleep继续持有PTY slave，移除了CI中约1.59秒Python冷启动对两秒行为期限的侵占；生产PTY代码、两秒请求期限、三秒总elapsed上限、exit/output及子进程阶段/PID断言不变。完整PTY runner 13项用3.80秒通过，父退出单项另连续5次用0.777–0.831秒通过，等待独立复审和原生macOS CI。
- Windows真实浏览器门禁现最多选择两个不同浏览器家族，Windows先运行与WebView2一致的Edge，只有进程级有界失败才尝试Chrome；每次仍保留45秒watchdog、10秒DOM期限、2MiB输出、隔离profile和进程树清理。清理失败会立即阻断，DOM/文字/布局断言也不会被第二个浏览器掩盖。初审发现显式`BROWSER_BIN`曾按路径本身去重，现改为按可执行文件basename/path识别Edge、Chrome与Chromium；发现层回归覆盖Edge/Chrome覆盖路径及同家族安装路径。三项失败先行回归转绿，25项生命周期及本机真实Chrome的8个Gemini状态、16组布局、632个文字角色通过，等待修复复审和原生Windows完整门禁。
- 修复后整体独立复审Critical/Important/Minor为0/0/0：浏览器显式覆盖优先、Windows标准Edge先于Chrome、跨平台路径家族识别、双重去重、最多两项、清理失败立即阻断和DOM后置断言均符合约束；未知wrapper保留为独立显式身份。macOS fixture也保持原两秒请求期限、三秒elapsed及exit/output/阶段/PID断言，审查者直接确认父进程0退出、后台子进程收到HUP后仍存活。审查者执行PTY runner 13项、生命周期25项、6份合同、发布脚本回归、207份文档、公开安全及diff检查全绿，放行完整门禁与原生CI。
- 本任务在复审后的最终源码上重新执行全部发布前门禁：Swift 458项普通+3项刷新调度+13项PTY runner+6项Gemini PTY共480项；Windows前端109项、协议夹具5项、生产构建、生命周期25项和真实Chrome的8个Gemini状态/16组布局/632个文字角色；完整宿主Rust 253项、格式与严格Clippy；6份合同、发布脚本回归、207份文档、公开安全及diff检查均全绿。无Widget macOS Release App另完成便携资源、嵌套代码签名和Sparkle更新Bundle验证，候选可提交并进入精确双平台原生CI。
- 精确候选`bdb1f2ec5fd1a84ed9653a7527405677210357a1`的macOS CI `34255362530`用2分37秒全绿；Windows CI `34255362553`已通过前端、真实浏览器、production build、格式和严格Clippy，但整项在9分46秒后失败，具体失败步骤尚未读取。先保留这一精确失败并读取完整日志，不把本机253项Rust或旧原生ConPTY成功作为放行；NSIS/PE是否执行也等待日志确认。
- Windows日志确认失败位于真实`gemini_conpty_windows`综合用例的`exit-conflict`场景，NSIS、GUI subsystem与安装包上传均按门禁跳过。夹具阶段完整到`model-closed>quit-received`，归一化输入是`/model\r`、Esc、`/quit\r`；16/1778/3859/7778/9164字节累计读取依次呈现Waiting、Ready、Waiting、Model=60%、Ready，但第116行没有得到该场景要求的UnrecognizedOutput。这排除命令未送达、模型页未解析与退出路径未完成；下一步核对退出冲突位于实时读取还是`stop()`尾段，并检查当前就绪队列排空是否漏掉冲突，继续维持8/2秒期限、60%、完整命令顺序和回收断言。
- 退出尾段回归用可控时钟精确模拟：子进程已被观察为退出后先连续两次空读，随后冲突帧才变为可读。按旧逻辑提取的收集循环会在第一次空读直接结束，测试稳定得到空尾段红灯；修复要求退出后连续静默100毫秒才结束，任何新字节都会重新计算静默窗口，同时仍使用5毫秒轮询、750毫秒总收集期限和256KiB尾段上限。回归现捕获延迟冲突帧，另一个可控时钟用例确认仍运行的进程恰在750毫秒停止；真实ConPTY断言也会在失败时打印实际返回值，子进程等待与回收路径未改。
- 修复后完整宿主Rust 256项通过，包含新增2项退出尾段单元回归、原有12项Gemini会话和1项大分片回归；格式、严格Clippy、6份跨平台合同、发布feed回归、Windows资产归一化回归、207份文档、公开发布安全和diff检查均为绿色，等待独立复审与原生Windows CI。
- 独立复审Critical/Important/Minor为0/0/0。审查者确认日志与fixture写入完成回调支持退出先于ConPTY尾帧可见这一根因，100毫秒连续静默会被任何新字节重置，750毫秒期限、256KiB上限及既有等待/Job Object回收保持有界；失败先行回归同时约束延迟尾帧和运行中进程期限。审查者实际执行2项定向回归、格式、严格Clippy、207份文档和diff检查全绿，并确认宿主Rust清单为256项；放行精确提交与原生Windows CI，最终结论仍以新提交CI为准。
- PR #13精确候选`ee59f267d3b01276b59ccfb4b46f6576a8b2de72`的macOS CI `34293609440`用2分39秒全绿，Windows CI `34293609443`用10分11秒通过真实浏览器、完整Rust/ConPTY、NSIS、GUI subsystem及产物上传；PR随后合并为main提交`849c4de1ad6b8454a944b2e776529531cff25ad3`。main macOS与Windows精确复验同时启动，Windows run `34294408821`在前端、浏览器、production build、格式和严格Clippy通过后于Test Windows runtime失败，后续NSIS/GUI/上传跳过；具体用例尚未读取。标签保持不存在，先保留失败并取证，不以PR分支绿灯替代main。
- Windows失败日志确认本轮尚未到达Gemini ConPTY：`cli_installation`四项先因真实PowerShell子进程路径用99.80秒全绿，下一测试程序`codex_collector`的有界app-server握手在约3.46秒返回TimedOut，其余3项通过，随后Cargo停止。需要检查三秒级握手期限与Windows冷启动/调度的关系并补足可诊断边界，不直接重跑。
- 同一main精确提交的macOS CI `34294408828`也在合并的Test and validate documentation步骤失败；toolchain与secret scanner已通过，具体用例尚未读取。标签继续保持不存在，先登记原始失败并取日志。
- macOS日志确认458项普通测试与3项刷新调度全绿，独立PTY runner的13项仅并发命令用例失败：约0.781秒后并发结果并非全部包含`user:<当前用户名>`，其余12项包括父退出、延迟尾段、超时及后代回收均通过。下一步增加结果级诊断并检查fixture/用户名环境，区分输出丢失与环境断言差异，不直接重跑。
- main两项失败都发生在测试专用用例，现修正测试边界并保留产品行为门禁，最终原因等待新CI判断。macOS旧并发用例没有结果级输出诊断，不能排除当次确有PTY输出丢失；但它把32路输出压力与`USER`/`NSUserName()`环境一致性混在一个断言中，现改用fixture固定`concurrent-output`标记，仍逐一断言32个结果、退出码0和标记完整，失败诊断只含索引/退出码/字节数，不暴露用户名或输出。Windows Codex集成用例的3秒参数短于产品真实10秒并把Node冷启动计入期限，现对齐产品10秒并在失败时打印实际耗时，生产采集代码与期限未变。PTY suite 13项、Codex集成4项全绿，并发单项另连续50轮通过，等待独立复审和新PR双平台门禁。
- 补丁独立复审最终Critical/Important/Minor为0/0/0：32路并发仍逐路要求固定正文完整并只输出索引、退出码与字节数诊断；Codex仅调整测试参数，生产入口仍为10秒。修正后完整本地门禁通过：Swift 458项普通+3项刷新调度+13项PTY runner+6项Gemini PTY共480项、宿主Rust 256项、格式、严格Clippy、6份跨平台合同、发布feed与Windows资产归一化回归、207份文档、公开发布安全及diff检查。下一步提交独立补丁PR，标签继续等待双平台和合并后main精确CI。
- 补丁提交`2f31b56c6c663427d4de47dcf2d5be6230e76118`的PR #14 macOS CI `34295964391`用2分8秒全绿，Windows CI `34295964457`用11分25秒通过完整运行时、真实ConPTY、NSIS、GUI subsystem及上传；合并为main提交`22cd6a3233448ec8a3cc517dfcdc3c17be95f6ac`后，macOS CI `34296804832`用2分36秒全绿，但Windows CI `34296804793`在前端、真实浏览器、production build、格式和严格Clippy通过后，于Test Windows runtime失败，NSIS/GUI/上传跳过，具体失败用例尚未读取。v0.6.0标签继续保持不存在，先取证，不以PR绿灯替代main精确复验。
- main Windows失败具体落在`claude_collector.authenticates_and_collects_through_a_real_conpty_session`：真实Claude ConPTY采集约5.16秒后返回TimedOut，同文件另外3项纯解析测试通过；Cargo随即停止，尚未运行后续Codex、Gemini及其他集成测试，NSIS/GUI/上传跳过。相同源码树在PR Windows CI完整通过，下一步比较两轮时序并补足启动、握手、输入、输出阶段诊断，保留5秒行为期限，不用直接重跑放行。
- 失败总耗时5.16秒与Claude采集唯一5秒阶段`auth status`吻合；后续首个ConPTY读取本应最多8秒，因此本轮尚未进入交互终端，而通过的PR同用例仅0.36秒。测试此前直接用外部Node解释器启动JavaScript夹具，和生产发现的Claude npm `.cmd`候选实际通过系统`cmd.exe`执行也不一致。修正会用原生批处理夹具同时响应`auth status`和ConPTY固定`/usage`，并写入固定阶段标记供失败诊断；产品5/8/12/8秒期限、采集代码和额度断言不变。
- 原生`.cmd`夹具修正独立复审Critical/Important/Minor为0/0/0：确认`auth status`与`--ax-screen-reader --safe-mode`路由、百分号、`set /p`交互、生产`/d /s /c`命令构造及固定marker诊断正确；成功仍必须完成认证、真实ConPTY、固定`/usage\r`并解析23% session与5% weekly。产品代码和5/8/12/8秒期限未改。本机完整宿主Rust 256项、格式、严格Clippy及Swift 458+3+13+6共480项、6份合同、feed/Windows资产、207份文档和公开安全门禁通过；原生`.cmd`/ConPTY继续等待Windows CI。
- Claude夹具提交`6ba3de42af122909ddf2024501ef7355c27966ad`的PR #15 macOS CI `34298032004`用2分17秒全绿，Windows CI `34298031988`用10分8秒通过完整运行时、真实ConPTY、NSIS、GUI subsystem及上传；合并为main提交`6e13c89ab133693431f8aa233227e1efa5dd5b9d`后，macOS CI `34298785597`在1分35秒于Test and validate documentation失败，具体用例尚未读取，Windows CI `34298785582`仍在运行。v0.6.0标签保持不存在，先取证并保留Windows独立结果。
- macOS固定标记诊断确认真实并发输出丢失：32路中的索引26、29、30、31都以exit 0在约0.628–0.634秒返回，但输出字节数为0；其余12项PTY runner、458项普通测试与3项刷新调度通过。旧用户名断言的不确定性已经消除，当前门禁准确捕获父进程正常退出时未收集PTY尾段；下一步检查`PTYCommandRunner`的退出观察、readability回调和关闭/排空顺序，以可控回归修产品代码，不删除并发压力或输出完整断言。
- 用户再次明确“确认发布”，公开main、`v0.6.0`标签、GitHub Release和应用内更新源授权继续有效。PTY语义探针进一步确认master在slave短暂无打开端时可先返回0，而同一slave稍后重新打开并写入后仍能被master读取；当前reader在退出信号前遇到0/EIO会立即返回，和CI的exit 0、零输出相符。下一步以可控状态回归锁定这一边界，并由进程退出完成直接触发原有有界排空。
- 修复现由退出完成回调在恢复等待者前直接发出descriptor排空信号；读取器在退出前遇到0/EIO继续等待可能重开的slave，退出后以100毫秒连续终端关闭静默收尾，并继续受750毫秒总期限、256KiB上限和10毫秒轮询约束。在当时父slave启动后即关闭的实现中，新fixture关闭子进程0/1/2、50毫秒后重开同一终端并写固定尾段，形成过真实的最后slave关闭窗口；PTY runner 16项全绿，原32路并发另连续50轮、重开尾段连续20轮通过。完整本机门禁为458+3+16+6共483项Swift、6份合同、feed/Windows资产、207份文档和公开安全检查；无Widget Release App的便携资源、Sparkle组件与严格签名验证也通过。main Windows run `34298785582`已通过完整runtime，正在构建NSIS，补丁仍等待独立复审和双平台原生CI。
- 独立初审报告Critical 0、Important 1：持续收到字节时尚未检查750毫秒deadline，只受256KiB上限；同时exitCode的快速读取理论上可发生在`onExit`之后。候选暂不提交，先用新增状态回归覆盖deadline后字节并调整完成发布顺序，确保时间与字节两个边界都真实成立，再交回原审查者复核。
- 精确main提交`6e13c89ab133693431f8aa233227e1efa5dd5b9d`的Windows CI `34298785582`最终用13分42秒全绿，包含前端、真实浏览器、production build、格式、严格Clippy、完整runtime和真实ConPTY、NSIS、GUI subsystem及安装包上传。该轮Windows基线可用，但同提交macOS仍有已定位PTY失败，不能创建标签；继续等待本补丁复审、PR双平台CI和合并后main复验。
- 初审两项回归先准确红灯：旧状态机在750毫秒deadline后收到字节仍KeepReading，旧waiter在`onExit`受控阻塞时会让`value()`快速路径先返回。修复把deadline统一置于所有读取结果分支前，readPTY仍先追加/解析当前批字节；two-phase完成用`isCompleting`单方认领handler/fallback，锁外执行退出动作后才发布exitCode并唤醒等待者。原审查者增量复核Critical/Important/Minor为0/0/0。修正后的完整本机门禁为458+3+17+6共484项Swift，最终压力为32路并发50/50、slave重开尾段20/20、退出发布顺序20/20；6份合同、feed/Windows资产、207份文档、公开安全及无Widget Release App的便携资源、Sparkle组件和严格签名验证均通过。候选可提交补丁PR。
- macOS PTY补丁精确提交`1d1c30ebf5ce827d1daaa9577db44696aecd6850`已推送并创建PR #16；macOS CI `34300285881`在1分52秒失败，具体步骤与用例尚未读取，Windows CI `34300285794`继续运行。先保存失败并读取日志，不原样重跑、不合并，也不创建`v0.6.0`标签。
- PR #16 macOS日志中458项普通、3项调度和PTY前16项均通过，只有32路并发失败：索引0–13正常，14–31共18路全部exit 0、bytes 0，耗时约0.785–1.501秒，形成固定执行宽度分界。`Task.detached(.utility)`当前调用不含await的同步reader，10毫秒`usleep`轮询会持续占用Swift协作线程；后续reader直到子进程关闭最后slave后才获得执行，而macOS会在此时丢弃未读PTY缓冲。修复需先确认reader已经启动并让轮询异步让出执行器；进程退出后继续保留父slave固定100毫秒，再关闭父slave并等待reader按100毫秒静默、750毫秒总期限与256KiB上限完成有界排空。延迟reader但父slave仍持有的快速输出回归用于锁定缓冲保留，不靠原样重跑。
- 延迟reader回归在旧候选上稳定得到exit 0但空输出，异步reader与父slave保护顺序完成后转绿。reader现在以`userInitiated`启动、进入循环前发出一次性信号，并在无数据时异步休眠10毫秒让出执行器；主流程确认reader已启动，进程退出后保留父slave 100毫秒，随后关闭父slave并等待reader的有界排空。完整本机门禁为458项普通、3项刷新调度、18项PTY runner和6项Gemini PTY，共485项Swift；32路并发压力100/100、延迟reader压力30/30，6份跨平台合同、Windows资产/feed回归、207份文档、公开发布安全和无Widget Release App的便携资源、Sparkle组件及严格签名验证均通过。PR #16旧提交的Windows CI `34300285794`也已完整通过；最终候选仍需更新PR并重跑精确双平台CI。
- 独立复审最初仅有2项Minor证据表述，现已全部关闭，最终Critical/Important/Minor为0/0/0。运行时集成测试准确描述子进程关闭并重开自身标准流，真实terminalClosed分支继续由独立状态机测试覆盖；历史旧实现与最终父slave保护顺序也已明确区分。命名和记录修正后，PTY runner 18/18、6份合同、feed/Windows资产、207份文档、公开安全与diff检查再次通过，可提交PR #16最终候选。
- 最终候选 `26d207b616f9f29f126ca226bc6d42136e4db75e` 的 [PR #16](https://github.com/sljzdotcom/AI-Token-Meter/pull/16) 门禁全绿：macOS CI `34301410029` 用 2 分 36 秒通过，Windows CI `34301410049` 用 10 分 11 秒通过严格 Clippy、完整 runtime、真实 ConPTY、NSIS、GUI subsystem 与产物上传。
- PR 合并为 main 提交 `94bf320c2d0ae033fd94ba20a711001be9dce0c4`；精确 main 复验中 macOS CI `34302154141` 用 3 分 20 秒通过，Windows CI `34302154157` 用 11 分 15 秒通过完整原生门禁。
- `v0.6.0` 由 [release workflow `34303172498`](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34303172498) 公开，tag 指向 `94bf320c2d0ae033fd94ba20a711001be9dce0c4`；macOS、Windows 与同步发布三项 job 全绿，发布后 appcast 提交为 `bacd138`。
- [GitHub Release v0.6.0](https://github.com/sljzdotcom/AI-Token-Meter/releases/tag/v0.6.0) 的 macOS ZIP 为 3,734,961 字节，SHA-256 `cf298d963ca3fb3a0add92a77cf2617ae07bdf28f9f70da3d9927c1f75844d3b`；Windows 安装器为 5,209,947 字节，SHA-256 `0f4b5a5823a970a6d7a668036bdd4b9006a84f75ad3ded276ed3f4e0aa578a47`。
- 全部公开资产已匿名重新下载；Sparkle ZIP 与 Windows updater 签名验证通过，篡改副本均被拒绝。稳定 appcast、Windows stable latest.json 与旧 Preview feed 均和 Release 资产完全一致，0.6.0 可由现有应用内更新入口发现。

原生 Windows SDK/ConPTY 已由 PR、main 与正式发布流水线验证。真实 Gemini 普通 OAuth 账号、真实额度与交互式 Windows GUI/DPI 保留现场边界；发布结果没有把这些现场事项改写为完成。
