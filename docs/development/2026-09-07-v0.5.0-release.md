# 2026-09-07：0.5.0 双平台稳定发布

关联 REQ-20260907-011；[计划](../design/implementation-plans/2026-09-07-v0.5.0-release.md)、[Release Notes](../releases/v0.5.0.md)。

## 准备

目标 0.5.0/macOS build13，基于 main `454d012`（CLI 引导 PR #10、About PR #11）。使用既有干净 main 顺序发布脚本，保留原更新源与两端公钥，不读取/导出私钥，不安装维护机器。

已于 2026-09-07 15:25:59（UTC+8）公开为 latest 稳定版：0.5.0/build13。公开前通道保持 0.4.0/build12，双平台签名资产完整后才推进更新入口。

第一次本机正式打包在主测试 419 项中被既有 `CLICollectorTests.codexTimeoutIsBounded` 拦截：约 1.924s 抛出函数级 transportFailure（第 226 行），与先前 CLI 引导记录的失败形态一致。独立核对确认这不是第 238 行 timedOut 断言不匹配：后续读取 fixture PID 的辅助函数也会抛出该错误，符合超时终止早于 fixture 写入 PID 的时序竞争，不能据此认定真实 CLI 超时分类错误。未创建 tag/草稿/公开资产；保留失败日志 `/private/tmp/v050-release-packaging.log`。原样定向复验 15 项全部通过（`/private/tmp/v050-cli-focused.log`），接着执行完整发布门禁，不跳过或放宽断言，不声称偶发问题根因已修复。

发布前发现 main Windows CI `34089957031` 的既有 `a_parent_exit_does_not_leave_a_descendant_running` 在三秒进程预算内返回 TimedOut；失败位于正常父进程完成断言，尚未证明子进程泄漏。该文件在 About 分支未改，PR Windows `34088933785` 相同产品树通过。作为 REQ-20260906-003 的新时序证据保留；本次标签发布门禁重新执行全部测试，不放宽/删除断言，也不把复验通过称作根因已修复。

## 验收边界

发布预检：80 项前端、21 项密度生命周期/632 项真实浏览器文字角色、production 前端构建、11 项 Swift 元数据、1 项 Rust 元数据以及 177 份文档/合同/公开安全检查通过。独立审查发现遗漏的 Rust 版本断言，已同步为 0.5.0 并通过聚焦测试；无其他发布阻塞。GitHub 草稿正文已使用本版 Release Notes，最终公开后同步当前版本指南。

只验证公开资产与更新发现链路，不代表已经在每台物理设备执行安装。macOS ad-hoc/未公证、Windows 无 Authenticode、Widget 延期及多屏/DPI 待验收状态保持不变。

## 正式构建与发布证据

- 本机完整原样复验 419 项主测试 + 13 项 PTY，共 432 项通过；Release 构建、便携资源、嵌套代码签名、Sparkle 签名与篡改拒绝均通过。日志 `/private/tmp/v050-release-packaging-recheck.log`。
- [Release workflow 34093997980](https://github.com/sljzdotcom/AI-Token-Meter/actions/runs/34093997980) 三项 job 全部成功：macOS 标签 432 项测试；Windows 80 项前端、21 项生命周期、632 项浏览器文字角色、218 项原生 Rust、Clippy、GUI subsystem=2、NSIS 与原公钥签名验证；最后公开并同步所有更新源。
- 版本准备提交 `13c12a2`，诊断记录/发布 Tag `de117b0c0f023ae6322d7d867c2e99f20211276a`（`v0.5.0`），appcast 提交 `f76ea0f`。未重写旧 Tag 或替换既有公开版本。
- [公开 Release](https://github.com/sljzdotcom/AI-Token-Meter/releases/tag/v0.5.0) 共七个资产：macOS ZIP/哈希/appcast、Windows EXE/哈希/签名/latest.json；`draft=false`、`prerelease=false`，匿名 GitHub latest API 返回 v0.5.0。

## 匿名公开下载与更新验收

不携带 GitHub 凭据重新下载七个资产及三个实际入口，输出 `/private/tmp/v050-public-verification.log`。全部通过：

| 资产 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| AI-Token-Meter-0.5.0-macOS-arm64.zip | 3642338 | `eb7dbd60476d4f3d7af265cf2f35f546e5dbd064a9d81a278d60b2c188097c80` |
| AI-Token-Meter-0.5.0-windows-x64-setup.exe | 5142461 | `9f574f677c53c437802bd692f928ce72fa750c3be23cc3bf94f1410a8d9ccea9` |

- macOS 公开 ZIP 与本机已验证归档逐字节一致；Sparkle 验签通过、篡改副本被拒绝，包内版本 0.5.0/build13，解包后 `codesign --verify --deep --strict` 通过。
- Windows 公开 EXE 通过 Tauri 原内嵌公钥验签；manifest 的签名与 `.sig` 完全一致，版本/下载 URL 匹配。
- [macOS 稳定 appcast](https://raw.githubusercontent.com/sljzdotcom/AI-Token-Meter/main/appcast.xml) 与公开 appcast 资产一致，首项 0.5.0/build13，URL/长度正确。
- [Windows stable](https://github.com/sljzdotcom/AI-Token-Meter/releases/latest/download/latest.json) 与[旧 Preview feed](https://github.com/sljzdotcom/AI-Token-Meter/releases/download/windows-preview-feed/latest-preview.json) 均与本版 latest.json 一致。
- 对比 v0.4.0 与本版元数据，更新入口、公钥、应用标识均未改变；旧 macOS 0.2.0 与 Windows Preview 路径也已核对。已有更新器的机器无需中间版本或重新配置；早期 0.1.2 没有更新器，仍需手动安装一次。

## 交付状态

REQ-20260907-011 已完成。CLI 引导（006/007）及 About 品牌（009）随本版交付；真实账号安装、Windows 多屏/DPI 和 Widget 的原有受限事项仍在需求台账独立跟踪。版本记录及 README 已同步；最终文档提交可通过本文件历史查询，不维护易过期的 HEAD 指针。
