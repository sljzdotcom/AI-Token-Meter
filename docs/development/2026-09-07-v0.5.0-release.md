# 2026-09-07：0.5.0 双平台稳定发布

关联 REQ-20260907-011；[计划](../design/implementation-plans/2026-09-07-v0.5.0-release.md)、[Release Notes](../releases/v0.5.0.md)。

## 准备

目标 0.5.0/macOS build13，基于 main `454d012`（CLI 引导 PR #10、About PR #11）。使用既有干净 main 顺序发布脚本，保留原更新源与两端公钥，不读取/导出私钥，不安装维护机器。

当前为待发布：公开通道仍为 0.4.0/build12。版本发布只在双平台签名资产完整后公开。

发布前发现 main Windows CI `34089957031` 的既有 `a_parent_exit_does_not_leave_a_descendant_running` 在三秒进程预算内返回 TimedOut；失败位于正常父进程完成断言，尚未证明子进程泄漏。该文件在 About 分支未改，PR Windows `34088933785` 相同产品树通过。作为 REQ-20260906-003 的新时序证据保留；本次标签发布门禁重新执行全部测试，不放宽/删除断言，也不把复验通过称作根因已修复。

## 验收边界

发布预检：80 项前端、21 项密度生命周期/632 项真实浏览器文字角色、production 前端构建、11 项 Swift 元数据、1 项 Rust 元数据以及 177 份文档/合同/公开安全检查通过。独立审查发现遗漏的 Rust 版本断言，已同步为 0.5.0 并通过聚焦测试；无其他发布阻塞。GitHub 草稿正文将使用本版 Release Notes，最终公开后再同步当前版本指南，不提前宣称交付成功。

只验证公开资产与更新发现链路，不代表已经在每台物理设备执行安装。macOS ad-hoc/未公证、Windows 无 Authenticode、Widget 延期及多屏/DPI 待验收状态保持不变。
