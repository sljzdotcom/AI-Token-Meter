# 移除展开态浮动条顶部横线

关联 REQ-20260908-011。用户于2026-09-08明确批准两端去掉顶部小横线；批准基线 `4cf994a`，本分支以 `e843750` 引入，接续REQ-010交付 `6917af8`。无需重复视觉选择。

## 方案与边界

只移除独立装饰：macOS `FloatingStripView` 展开分支顶部18×3 Capsule及其专用容器；Windows `FloatingStrip` 的 aria-hidden drag-handle元素及专用基本、hover和左边镜像样式。实际拖动由背景命中区域/父容器处理，装饰不承担行为；不通过隐藏整个顶部区域或调整内容高度实现。

两端 Compact/Comfortable、左/右贴边的展开态均无横线；背景与外轮廓、Logo/间距/窗口尺寸保持不变。非按钮背景仍可拖动，Provider仍可点击且不触发拖动；左右吸附、多屏与位置持久化保持原逻辑。折叠态3×40竖线和展开操作保留，不移除其他分隔线。

## 验收

测试先行确认旧视图有该横线而不满足新视觉要求，再以最小删除转绿。Windows覆盖实际组件渲染、左右/密度、背景指针拖动事件与Provider点击隔离；macOS以真实渲染图像或原生视图证据确认顶部装饰消失，运行命中/拖动/Provider布局相关回归。不得仅凭源码文本缺失声称完成视觉/交互验证。

完整测试前运行文档门禁；本机Swift全套、Windows前端全套及生产构建、宿主Rust回归、Release编译和独立审查提供交付证据。原生Windows WebView2/DPI和真实多屏现场验收边界如实保留。

本次仅本地开发；不发布、不安装替换应用、不改版本/签名/feeds、账号或软件更新行为，不覆盖主工作区或REQ-010记录。
