# AI Token Meter 0.7.2 详情与Compact稳定修复发布计划

**需求：** `REQ-20260910-008`  
**交付：** `REQ-20260910-006`、`REQ-20260910-007`  
**授权：** 用户明确要求修复后直接发布，不再询问。

**目标：** 发布双平台稳定版0.7.2、macOS build21，让现有macOS与Windows安装取得Antigravity详情底板修复和56.5pt/px Compact浮动条。

**版本决定：** 两项均为向后兼容的视觉缺陷修复，在已公开0.7.1/build20基础上使用PATCH递增0.7.2和单调build21；不移动或替换任何既有标签。

## 任务1：版本与发布资料

- [x] 核对公开0.7.1、当前main、需求授权和交付范围，选定0.7.2/build21。
- [x] 版本合同先期待0.7.2/build21并取得旧0.7.1/build20失败，再同步根版本、两份macOS plist及Windows npm/Cargo/Tauri。
- [x] 创建Release Notes、发布记录、Changelog和文档索引。

## 任务2：候选门禁与审查

- [x] 运行完整Swift、跨平台合同、文档、公开安全、Windows前端、真实浏览器、production build、Rust、格式和严格Clippy。
- [x] 构建0.7.2/build21无Widget Apple Silicon Release App，验证便携资源、版本、Sparkle组件和严格签名。
- [x] 复核差异、版本单调性、交付范围、凭据边界、回滚和公开说明，关闭全部审查发现。

本机候选通过464项普通Swift、3项独立刷新调度和18项PTY runner，共485项；Windows前端124项、宿主Rust 242项、25项浏览器生命周期、8种Antigravity详情状态、9种详情表面、16组浮动条和608个文字角色通过。production build、Rust格式、全目标全功能严格Clippy、6份跨平台合同、254份Markdown、公开安全和0.7.2/build21无Widget arm64严格签名Release App全部通过。首次受限Rust运行仅有6项隔离测试服务器因沙箱禁止回环绑定失败，允许回环后原命令完整通过。最终差异审查Critical/Important/Minor为`0/0/0`。

## 任务3：PR与main

- [ ] 推送发布分支并创建PR，等待精确候选macOS与Windows原生CI通过。
- [ ] 合并到main并等待精确合并提交双平台CI再次通过。

## 任务4：签名发布事务

- [ ] 只从干净且与远端一致的main执行`scripts/package-cross-platform-release.sh 0.7.2 21`。
- [ ] 等待标签workflow验证macOS资产、Windows签名NSIS、PE GUI subsystem和内嵌公钥验签，随后公开Release并推进三个更新源。
- [ ] 任何失败按事务状态恢复，不强推、不替换已公开字节、不绕过签名或门禁。

## 任务5：公网验收与结项

- [ ] 无认证下载全部公开资产，核对文件名、大小、SHA-256、Sparkle/Tauri签名和篡改拒绝。
- [ ] 核对根appcast、Windows stable和旧Preview feed都指向0.7.2，并验证旧版本可发现更新。
- [ ] 记录Release、tag、workflow、CI、资产哈希和现场边界；提交发布后证据并通过双平台CI。
- [ ] 公开资产、更新源和发布后证据CI全部通过后关闭REQ-008。
