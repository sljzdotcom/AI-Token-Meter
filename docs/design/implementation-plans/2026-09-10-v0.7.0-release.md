# AI Token Meter 0.7.0 Google Antigravity 与紧凑界面稳定发布计划

**需求：** `REQ-20260910-004`  
**交付：** `REQ-20260909-007`、`REQ-20260909-011`、`REQ-20260910-001`  
**授权：** 用户在确认Compact采用65pt后明确要求“请发布”。

**目标：** 发布双平台稳定版0.7.0、macOS build19，让现有macOS与Windows安装通过应用内更新取得Google Antigravity迁移、40pt菜单面板Logo和65pt Compact浮动条。

**版本决定：** 第四项服务的采集入口和额度模型由旧Gemini CLI迁移到Google Antigravity，属于用户可见功能变化，因此使用0.7.0而非补丁版本；macOS build从18单调递增到19。

## 任务1：版本与发布资料

- [x] 核对最新main、远端标签、Release、发布授权和签名工具可用性，确认0.7.0/build19未占用。
- [x] 先把版本合同改为0.7.0/build19并取得失败证据，再同步根版本、两份macOS plist、Windows npm/Cargo/Tauri与锁文件。
- [x] 创建Release Notes与发布记录，把Unreleased内容移入0.7.0，更新README、项目状态、测试/发布文档和索引。

失败先行证据：Swift版本合同7处收到0.6.3/build18而期望0.7.0/build19，Rust应用元数据测试收到0.6.3而期望0.7.0；同步生产元数据后，Swift定向13项与Rust元数据1项全部通过。

## 任务2：候选门禁与审查

- [x] 运行完整Swift、跨平台合同、文档、公开安全、Windows前端、真实浏览器、production build、Rust、格式和严格Clippy。
- [x] 构建0.7.0/build19无Widget Apple Silicon Release App，验证便携资源、版本、Sparkle组件和严格签名。
- [x] 复核版本单调性、交付范围、更新兼容、凭据边界、回滚与公开说明，记录审查结论。

本机候选`9bdc352`通过483项Swift、124项Windows前端、241项宿主Rust、25项浏览器生命周期、8个Antigravity状态、16组布局与608个文字角色；production build、Rust格式、严格Clippy、6份跨平台合同、243份Markdown、公开安全及0.7.0/build19 arm64严格签名Release App均通过。结构化差异复核结论为Critical/Important/Minor `0/0/0`。

## 任务3：公开候选与main

- [ ] 推送发布分支并创建PR；等待精确候选的macOS与Windows原生CI全部通过。
- [ ] 合并到main并等待精确合并提交的双平台CI再次通过。

## 任务4：签名发布事务

- [ ] 只从干净且与远端一致的main执行`scripts/package-cross-platform-release.sh 0.7.0 19`。
- [ ] 等待标签workflow验证macOS资产、Windows签名NSIS、PE GUI subsystem、内嵌公钥验签并公开Release与三个更新源。
- [ ] 任何失败按事务状态恢复，不强推、不替换已公开字节、不绕过签名或门禁。

## 任务5：公网验收与结项

- [ ] 无认证下载全部公开资产，核对文件名、大小、SHA-256、Sparkle/Tauri签名和篡改拒绝。
- [ ] 核对根appcast、Windows stable与旧Preview feed都指向0.7.0，并验证现有版本可发现更新。
- [ ] 记录Release、tag、workflow、CI、资产哈希与现场边界；提交发布后证据并等待main双平台CI。
- [ ] 公开资产、更新源和发布后CI全部通过后，关闭REQ-004及已交付需求。
