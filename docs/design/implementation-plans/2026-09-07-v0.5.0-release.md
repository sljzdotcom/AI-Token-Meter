# 0.5.0 双平台稳定发布计划

关联 REQ-20260907-011。用户明确授权发布，沿用现有发布流程，不新增产品行为或更换签名信任根。

本轮为顺序发布操作，既有脚本强制干净 main；直接沿用已确认的 main 发布入口，不创建另一份功能工作树。所有脚本在执行前审阅，私钥不导出。

## 执行步骤

- [x] 同步 VERSION、两端元数据和版本断言为 0.5.0/macOS build13，补 Release Notes、CHANGELOG、指南和文档索引。资产公开前标注待发布。
- [x] 文档/合同/公开安全、Windows 前端/密度/构建通过；提交版本检查点。既有终端时序失败独立留档，不跳过断言。
- [x] 干净 main 执行 `SPARKLE_TOOLS_DIR=/absolute/Sparkle/bin scripts/package-cross-platform-release.sh 0.5.0 13`，完整测试、构建、Keychain 签名后创建 tag 与草稿，触发既有 release.yml。
- [x] 双平台标签门禁及 Windows 签名 NSIS 成功后由 workflow 公开资产并更新 macOS appcast、Windows stable/legacy Preview feed；失败遵循脚本补偿流程。
- [x] 匿名重新下载公开资产和三个入口，验证版本/build 单调递增、SHA-256、Sparkle/Tauri 签名、URL、公钥和旧版入口兼容；记录证据并完成需求。

完成：2026-09-07，Tag `de117b0`、appcast `f76ea0f`、workflow `34093997980`；[完整证据](../../development/2026-09-07-v0.5.0-release.md)。

## 边界

不自动安装维护机器，不操作用户账号。macOS 仍 ad-hoc/未公证、不含 Widget；Windows updater 签名不等于 Authenticode。原有真机多屏/DPI 与偶发终端时序问题保持独立状态，不因发布自动完成。
