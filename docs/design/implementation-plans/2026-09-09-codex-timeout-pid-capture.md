# Codex 超时进程取证稳定性实现计划

> **面向 AI 代理的工作者：** 在长期开发对话内顺序执行；每个发布检查点以新鲜证据放行。

**目标：** 消除 Codex 超时回收测试对子进程先写 PID 文件的调度依赖，同时完整保留超时、总预算和真实进程退出断言，使 0.6.3 发布门禁可以可靠判断产品行为。

**架构：** 在 `CodexAppServerClient` 的进程启动与清理登记点增加默认为空的内部 PID 观察闭包；测试通过锁保护的内存捕获器取得 PID。生产调用不传闭包，命令、协议、期限和清理逻辑保持。

**技术栈：** Swift 6、Foundation `Process`、Swift Testing、SwiftPM。

---

## 任务1：登记、失败证据与设计

- [x] 登记 `REQ-20260909-009`，记录首次正式打包停止于标签与 Release 创建之前。
- [x] 对照既往同形失败与当前测试，确认失败发生在预期超时后的 PID 文件读取，而不是产品没有超时。
- [x] 写明父进程启动点直接取 PID 的方案、顺序、不变量和发布放行标准。

## 任务2：失败先行与最小实现

- [x] 先改测试使用内存 PID 捕获器，取得缺少 `processDidLaunch` 参数的编译失败。
- [x] 在进程成功启动并登记到清理容器后传出 `processIdentifier`，默认闭包为空。
- [x] 保留预期 `timedOut`、小于 2 秒总耗时和 2 秒内真实进程退出断言。
- [x] 运行单项及全部 `CLICollectorTests`，确认 1/1 与 16/16 通过。

## 任务3：完整验证与审查

- [x] 补齐开发记录和 v0.6.3 首次打包失败记录，运行文档、公开安全与差异检查。
- [x] 运行正式脚本使用的完整 Swift 门禁，并重新构建、验证 0.6.3/build18 无 Widget Release App。
- [x] 审查进程登记顺序、线程安全、期限、清理、生产默认路径和测试证据；记录 Critical/Important/Minor 结果。
- [ ] 提交修复并更新需求台账证据。

## 任务4：PR、主分支与恢复发布

- [ ] 推送修复分支并创建目标为 `main` 的 PR，等待精确 macOS 与 Windows CI 全绿。
- [ ] 合入 `main`，等待精确合并提交的双平台 CI，再安全同步主工作区并保留协调入口 stash。
- [ ] 从干净且与远端一致的 `main` 重新运行 0.6.3 正式发布事务；完整公网验收和发布后证据由 `REQ-20260909-008` 计划继续追踪。
