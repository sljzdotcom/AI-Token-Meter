# AI Token Meter 0.6.3 macOS About 精简稳定发布计划

> **面向 AI 代理的工作者：** 在长期开发对话内顺序执行；每个检查点以新鲜验证证据放行。用户已明确回复“确认发布”，授权把已完成的 About 精简生成可供现有设备更新的新版本。

**目标：** 将 `REQ-20260909-006` 的 macOS About 独立作者行移除交付为双平台稳定版0.6.3（macOS build18），让现有安装通过应用内更新取得。

**架构：** 从已验证并本地整合的 `1015ea3` 建立补丁发布候选；同步根版本、macOS plist、Windows npm/Cargo/Tauri、测试合同及公开文档，再通过本机全套、审查、PR与合并后原生双平台CI。只从干净且与远端一致的公开 `main` 创建注解标签和签名资产；release workflow验证同一标签的macOS Sparkle包与Windows Tauri/NSIS包后公开，并推进稳定appcast、Windows stable与旧Preview feed。

`REQ-20260909-007` 尚需确认Google Antigravity与Gemini CLI的官方产品关系和额度语义，因此不在缺少官方结论时混入本补丁版。

**技术栈：** Swift 6/SwiftPM、Tauri 2、Rust 1.88、React/Vitest、GitHub Actions、Sparkle 2.9.4、Tauri minisign、NSIS。

---

## 任务1：授权、版本与发布资料

- [x] 核对唯一需求台账、协调绑定、执行所有者和用户明确发布授权。
- [x] 拉取远端标签与Release列表，确认最高稳定版为0.6.2、0.6.3未被占用；确定macOS build从17单调递增到18。
- [x] 先把元数据测试改为0.6.3/build18并取得预期RED，再同步根版本、macOS plist、Windows npm/Cargo/Tauri及锁文件取得GREEN。
- [x] 把Unreleased About条目移入0.6.3，创建Release Notes和开发记录，更新README、用户指南、项目状态、测试/发布文档与索引。
- [x] 运行版本合同、文档、公开安全和diff检查，提交候选资料。

## 任务2：本地完整门禁与审查

- [x] 顺序运行完整Swift测试，包括普通组、刷新调度、PTY runner与Gemini PTY；保留断言、期限和跳过边界。
- [x] 运行Windows前端、终端协议、生产构建、真实浏览器密度、宿主Rust全套、格式与全目标严格Clippy。
- [x] 构建0.6.3/build18无Widget macOS Release App，验证便携资源、Sparkle framework/helper、`@rpath`、版本和严格嵌套签名。
- [x] 对版本、范围、更新单调性、About行为、发布脚本、隐私、回滚与现场边界做独立发布审查；修复发现并重跑受影响门禁。

## 任务3：公开候选、PR与主分支

- [ ] 推送 `codex/v0.6.3-release` 并创建目标为 `main` 的PR，准确写明About前后行为与验证结果。
- [ ] 等待精确候选的macOS与Windows CI全绿；Windows必须通过严格Clippy、完整runtime、真实ConPTY、NSIS、GUI subsystem和安装器上传。
- [ ] 若CI失败，先定位并修复真实原因，不跳过测试、不降低门禁、不复用失败候选证据。
- [ ] 合入公开 `main`，等待精确合并提交的双平台CI再次全绿；同步本地主工作区并保留协调入口与既有stash。

## 任务4：签名资产与公开事务

- [ ] 只检查Sparkle Keychain签名工具/公钥和GitHub两项Tauri Secret是否可用，不读取、导出或输出私钥与密码。
- [ ] 在干净且与远端一致的 `main` 使用绝对 `SPARKLE_TOOLS_DIR` 执行 `scripts/package-cross-platform-release.sh 0.6.3 18`。
- [ ] 等待发布workflow验证标签源码、草稿macOS资产、原生Windows签名NSIS、PE GUI subsystem、内嵌公钥验签、事务式公开与feed推进。
- [ ] 任何失败按脚本补偿状态处理；不强推、不重用版本、不替换已公开同版本字节，也不绕过签名或更新单调性。

## 任务5：公网验收与收尾

- [ ] 无认证下载0.6.3全部资产，核对文件名、大小、SHA-256、Sparkle EdDSA、Tauri minisign和篡改副本拒绝。
- [ ] 核对根 `appcast.xml`、Windows stable `latest.json` 与固定 `windows-preview-feed/latest-preview.json` 都指向公开0.6.3资产，版本/build高于0.6.2/build17。
- [ ] 记录Release、tag/合并/appcast SHA、workflow、CI、资产哈希、更新发现、签名与真机边界；更新需求台账和版本文档。
- [ ] 运行发布后文档、公开安全、合同与diff检查，提交证据并等待精确 `main` 双平台CI。
- [ ] 只有公开资产、三个更新源和发布后CI全部通过后，才把REQ-008标为已完成并向协调入口回传。
