# 0.4.0 双平台稳定发布计划

关联需求：REQ-20260907-005。用户已授权直接发布，不增加产品设计或变更签名信任根。

## 执行步骤

1. 核对 main 双平台 CI；同步 VERSION、macOS 两份 plist/build 12、Windows npm/Cargo/Tauri 与版本断言，补充 CHANGELOG、Release Notes 和文档索引。
2. 运行文档、合同、公开安全、Windows 前端/字号/构建验证；提交发布元数据。此阶段仍明确为待发布。
3. 干净 main 使用既有跨平台发布脚本完整运行 macOS 测试、构建及 Keychain 签名，推送 v0.4.0 并创建草稿；Windows 私钥仅由现有 Actions Secrets 使用。
4. 等待标签上的双平台门禁和签名安装包构建全部通过，由 workflow 公开资产后更新 macOS appcast、Windows stable/legacy Preview feed。
5. 匿名重下安装包和三个 feed，验证 SHA-256、Sparkle/Tauri 签名、版本和下载地址；记录工作流、Tag 与哈希证据，然后更新需求状态并提交。

## 失败与边界

- 任一构建/签名门禁失败不公开；公开后的失败遵循现有 feed 补偿流程，不静默替换同版本安装包。
- Widget 证书仍延期；macOS ad-hoc/未公证、Windows 无 Authenticode 的提示保持不变。
- 物理双屏/DPI/字体和偶发终端时序问题保持独立追踪，不因本次发布标为完成。
- 本次不在维护机器自动安装，不操作用户凭据或重置券。
