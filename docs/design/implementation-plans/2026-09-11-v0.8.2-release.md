# AI Token Meter 0.8.2 悬浮条即时密度与轮廓修复稳定发布计划

**需求：** REQ-20260911-016

**交付：** REQ-20260911-013至REQ-20260911-015
**授权：** 用户确认发布完整双平台稳定版，并允许公开候选源码、PR、main合并、`v0.8.2`标签、GitHub Release、现有签名流程和三个更新源。

**目标：** 发布双平台稳定版0.8.2、macOS build25，让已展开悬浮条的密度设置立即改变原生窗口尺寸，并在左右贴边时同时保留暴露侧圆角和清晰、严格镜像的反向肩弧。

## 候选

- [x] 同步0.8.2/build25版本合同、两份macOS plist和Windows npm/Cargo/Tauri元数据。
- [x] 创建Release Notes、开发记录、Changelog和文档索引。
- [x] 运行完整Swift、跨平台合同、文档、公开安全、Windows前端、真实浏览器、production build、Rust、格式、严格Clippy与无Widget Release App验证。
- [x] 完成独立差异审查并关闭全部Critical、Important和Minor发现。

## 发布事务

- [x] 推送候选PR，等待精确候选macOS与Windows原生CI通过后合入main。
- [x] 等待精确main合并提交的双平台CI通过。
- [x] 只从干净且与远端一致的main执行发布脚本，创建`v0.8.2`与草稿Release。
- [x] 等待标签workflow验证并公开macOS与Windows签名资产，推进三个更新入口。
- [x] 匿名重下全部七项资产，核对SHA、两端签名、篡改拒绝、版本元数据和更新源。
- [x] 提交发布后证据，通过双平台CI后关闭需求。

## 验收边界

- GitHub Windows runner提供原生编译、测试和NSIS资产证据；物理Windows 11多屏、真实指针与125%/200% DPI继续作为现场观察边界。
- macOS自动化覆盖SwiftUI/AppKit结构、绘制、窗口控制和真实`NSPanel`尺寸变化；不把未执行的人工视觉观察写成已验证事实。
- Windows仍无Authenticode；macOS公开包仍为Apple Silicon、ad-hoc、未公证且不含Widget。
