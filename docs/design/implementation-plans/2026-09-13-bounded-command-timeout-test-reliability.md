# BoundedCommandRunner停止测试可靠性实现计划

**目标：** 让超时与取消回归测量真实停止请求到操作系统进程退出的耗时，消除异步任务调度造成的误报。

**范围：** macOS核心runner的内部测试观察入口、对应测试、发布证据与需求记录；生产超时和进程清理逻辑不变。

## 任务1：失败证据与设计

- [x] 核对CI日志，确认预期`timedOut`已经返回、唯一失败为完整墙钟1.507秒，且日志不能区分触发前等待与触发后清理。
- [x] 本机低负载连续20次通过，并对照已有取消测试的真实事件计时模式。
- [x] 记录测量缺陷、证据边界、最小观察入口及不变量。

## 任务2：测试驱动修正

- [x] 先改测试使用超时事件观察入口，确认旧实现编译失败。
- [x] 实现默认关闭的内部观察入口，保留公开初始化与全部生产行为。
- [x] 运行单项连续回归及完整Swift门禁。

## 任务2A：取消返回与进程退出解耦

- [x] 登记PR #44后续macOS CI的1.105秒取消墙钟失败，恢复REQ-20260912-006为进行中。
- [x] 先让超时和取消测试调用尚不存在的进程退出观察入口，确认旧实现编译失败。
- [x] 复用既有只触发一次的进程退出事件，分别断言停止后一秒内退出与调用最终返回正确错误。
- [x] 四项定向测试及连续20轮回归通过。

## 任务3：审查与集成

- [x] 更新需求、发布与提交证据，运行文档、公开安全和差异检查。
- [x] 完成独立审查并关闭全部Critical、Important与Minor问题。
- [x] 精确候选通过PR #44双平台CI后合入main，并在结项证据中记录最终main CI。
