# AI Token Meter 项目工作规则

## 需求登记优先

处理任何新的用户需求、问题、想法、缺陷或验收反馈前，先读取并更新 `docs/requirements-backlog.md`。

1. 新事项使用 `REQ-YYYYMMDD-NNN` 标识，保留用户意图、登记日期、类别、优先级和验收口径。
2. 状态只使用：`待处理`、`进行中`、`待用户确认`、`受环境限制`、`已延期`、`已完成`。
3. 用户在进行中的任务期间补充新想法时，先登记；除非用户明确要求改变优先级，否则继续完成当前任务，再按列表顺序处理。
4. 需要用户作视觉或业务选择时标记 `待用户确认`，并写明等待的具体决定。
5. 完成后标记 `已完成`，记录完成日期、规格/计划/开发日志和 Git 提交证据。
6. 不删除已完成或已延期事项；保留它们作为需求历史。内容过长时按年份归档，并在主列表保留索引。
7. 每个工作阶段结束前重新读取列表，确认是否还有可继续处理的 `进行中` 或 `待处理` 项。

## 开发纪律

- 遵循仓库现有的设计规格、测试驱动、验证和 Git 检查点流程。
- 不把 `受环境限制` 或 `已延期` 冒充为 `已完成`。
- 新增文档时同步维护 README 或文档索引，保证需求列表始终容易找到。
- 完整测试与发布前运行 `scripts/check-docs.sh`；设计规格和计划只存放在 `docs/design/`，不得重建第二需求列表或 `docs/superpowers/`。

## 需求对话与开发对话分工

- 用户日常提需求的对话负责登记、优先级、派工、汇总结果和请求用户验收；长期开发对话负责实现、测试、文档和代码交付。
- 两个对话共用唯一需求台账，不凭聊天记忆重做已完成需求。具体协作规则见 `docs/development/agent-collaboration.md`。
- 主工作区的 `.agent-coordination.local.json` 保存本机对话绑定（已忽略，不提交）。派工前读取绑定并核对对话状态；文件缺失时用项目对话列表重新定位，不凭空编造 ID。
- 派工需携带需求 ID、已提交的需求/规格基线、范围、验收口径和结果回传目标。开发对话在自己的工作区修改，不覆盖主工作区或另一个对话的未提交改动。
- 开发完成或遇到需用户决定的问题时，将测试证据、提交和阻塞回传需求对话；默认技术决策自行处理，重大分歧或视觉选择才交用户。
- 运行中的新需求先排队，不重复启动同一开发工作。不将待确认/受限/延期事项擅自视为可执行任务；“不发布”的约束跨对话继续有效。
